//
//  ManualWallSelector.swift
//  OBOIA
//
//  Manual wall selection fallback (stub — full implementation in later phase)
//

import UIKit

// `slog` is used by the old code; we keep a no-op to avoid compile errors.
func slog(_ items: Any...) {
    // logging removed for now
}

final class ManualWallSelector {
    // This class will be properly implemented when we add manual wall mode.
    // For now it exists only to satisfy the Xcode project requirements.
    init() {}
}
